[project]
name = "tts-cloning-voice"
version = "0.1.0"
description = "Using TTS to make intro of TikTok Videos"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chatterbox-tts>=0.1.7",
    "peft>=0.19.1",
    "soundfile>=0.13.1",
    "torch>=2.6.0",
    "torchaudio>=2.6.0",
]
