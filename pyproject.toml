[project]
name = "tts-fish-speech"
version = "0.1.0"
description = "Using Fish Speech to make intro of TikTok Videos"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fish-audio-sdk>=1.3.0",
]
